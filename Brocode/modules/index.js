import * as mathUtil from "./math.util.js";

console.log(mathUtil.PI);

let circumference = mathUtil.getCircumference(12);
console.log(circumference);

let area = mathUtil.getArea(10);
console.log(area);